package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sleek Interface Theme Color Palette
val PrimarySleek = Color(0xFF00B0FF)       // Light cyber blue
val SecondarySleek = Color(0xFF3D5AFE)     // Accent indigo blue
val AccentOrange = Color(0xFFFF6D00)       // Live badge orange
val SleekTextSecondary = Color(0xFF9EA1A4) // Soft grey for secondary text
val SleekBorderColor = Color(0x0DFFFFFF)   // border-white/5 for clean separators

// Dark Theme Colors
val DarkBackground = Color(0xFF0F1113)    // Sleek deep charcoal background
val DarkSurface = Color(0xFF1A1C1E)       // Sleek card and container surface
val DarkSurfaceVariant = Color(0xFF23262B) // Elevated list card highlights
val OnDarkBackground = Color(0xFFE2E2E6)   // Sleek bright grey-white text
val OnDarkSurface = Color(0xFFFFFFFF)      // Pure white text

// Light Theme Colors (Clean fallbacks retaining sleek geometry)
val LightBackground = Color(0xFFF4F6F9)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFE5E9F0)
val OnLightBackground = Color(0xFF0F1113)
val OnLightSurface = Color(0xFF0F1113)
